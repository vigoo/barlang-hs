+ Command line arguments, compile & pprint
+ Bool
+ int
+ double
+ Nesting numeric and boolean expressions
  + subexprs must also return prereqs
  + refactor bool/int/double subexpr entry points to be reusable from each other
+ str()
+ Auto inline
+ If
+ better str()
* Closure
- Process
- Arrays, command line args
* Loop
- Encode/decode mes source to bash as comment
- Traps
- Import
- Integration test framework
- Type alias (for example double represented as string, wrappers around bc, etc.)
- Regexp
